const myString = 'developer';

let u = myString[0].toUpperCase();
let r = myString.slice(1);
let myNewString = u + r;

// const myNewString = `${myString[0].toUpperCase()}${myString.slice(1)}`;

console.log(myNewString);